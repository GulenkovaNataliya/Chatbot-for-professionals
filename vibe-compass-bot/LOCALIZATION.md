# Инструкция по локализации бота

## Как переключить бота на греческий язык

### Способ 1: Простое переключение (рекомендуется для начинающих)

1. Откройте файл `handlers.py`
2. Найдите строку (в самом начале):
```python
from messages import *
```

3. Замените её на:
```python
from messages_el import *  # для греческого
```

4. Перезапустите бота:
```bash
python bot.py
```

Готово! Теперь бот говорит по-гречески.

### Способ 2: Конфигурируемый выбор языка

Создайте систему выбора языка через конфигурацию:

1. Откройте `config.py` и добавьте в самом начале:
```python
# Язык бота: 'ru', 'en', 'el' (греческий)
LANGUAGE = 'ru'
```

2. Откройте `handlers.py` и замените импорт:
```python
# Было:
from messages import *

# Стало:
from config import LANGUAGE

if LANGUAGE == 'ru':
    from messages import *
elif LANGUAGE == 'el':
    from messages_el import *
elif LANGUAGE == 'en':
    from messages_en import *  # если создадите английскую версию
else:
    from messages import *  # по умолчанию русский
```

3. Теперь для смены языка просто меняйте `LANGUAGE` в `config.py`

### Способ 3: Динамический выбор языка пользователем

Для продвинутых пользователей - позвольте каждому пользователю выбрать язык:

1. Добавьте поле `language` в модель `User` в `database.py`:
```python
class User(Base):
    # ... существующие поля
    language = Column(String(10), default='ru')
```

2. Создайте функцию выбора языка в `handlers.py`:
```python
async def select_language(update, context):
    keyboard = [
        [InlineKeyboardButton("🇷🇺 Русский", callback_data='lang_ru')],
        [InlineKeyboardButton("🇬🇷 Ελληνικά", callback_data='lang_el')],
        [InlineKeyboardButton("🇬🇧 English", callback_data='lang_en')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    await update.message.reply_text("Выберите язык / Επιλέξτε γλώσσα / Choose language:", reply_markup=reply_markup)
```

3. Загружайте тексты динамически на основе языка пользователя

## Создание новой локализации

### Для английского языка:

1. Скопируйте `messages.py` в `messages_en.py`
2. Переведите все тексты на английский
3. Используйте способ 1 или 2 для подключения

### Для любого другого языка:

Пример для испанского:
```bash
# Скопируйте файл
copy messages.py messages_es.py  # Windows
cp messages.py messages_es.py    # Linux/Mac

# Откройте и переведите все тексты
# Подключите через config.py или handlers.py
```

## Структура файла локализации

Каждый файл локализации должен содержать:

1. **START_MESSAGE** - приветственное сообщение
2. **QUESTION_1_MESSAGE** - вопрос об эмоциях
3. **EMOTION_RESPONSES** - ответы на выбор эмоций
4. **QUESTION_2_MESSAGE** - вопрос о проблеме
5. **QUESTION_3_MESSAGE** - вопрос о времени
6. **INSIGHTS** - персонализированные инсайты для каждой проблемы
7. **get_insight_message()** - функция генерации инсайта
8. **OFFER_MESSAGE** - план освобождения
9. **CONVERSION_MESSAGE** - сообщение после заявки
10. **PDF_MESSAGE** - сообщение при скачивании PDF
11. **REMIND_LATER_MESSAGE** - напоминание
12. **THANK_YOU_MESSAGE** - благодарность

## Важные моменты при переводе

### Эмодзи
Эмодзи можно оставлять как есть - они универсальны:
```python
"Привет! 👋"  # работает на всех языках
```

### Кнопки клавиатуры
Не забудьте перевести тексты кнопок в `keyboards.py`:

```python
# keyboards_el.py (греческий)
def get_start_keyboard():
    keyboard = [
        [InlineKeyboardButton("✨ Ναι, πάμε!", callback_data='start_quiz')],
        [InlineKeyboardButton("⏰ Αργότερα...", callback_data='remind_later')]
    ]
    return InlineKeyboardMarkup(keyboard)
```

### Callback данные
**НЕ ПЕРЕВОДИТЕ** callback_data - они должны оставаться на английском:
```python
# ПРАВИЛЬНО ✅
InlineKeyboardButton("Ναι, πάμε!", callback_data='start_quiz')

# НЕПРАВИЛЬНО ❌
InlineKeyboardButton("Ναι, πάμε!", callback_data='αρχη_τεστ')
```

## Тестирование локализации

1. Запустите бота с новой локализацией
2. Пройдите весь сценарий от начала до конца
3. Проверьте:
   - Все тексты отображаются правильно
   - Кнопки работают
   - Персонализированные инсайты генерируются корректно
   - Нет пропущенных переводов

## Примеры готовых локализаций

### Доступные языки:
- ✅ `messages.py` - Русский (основной)
- ✅ `messages_el.py` - Греческий (Ελληνικά)
- ⏳ `messages_en.py` - Английский (планируется)
- ⏳ `messages_es.py` - Испанский (планируется)

## Советы по переводу

1. **Сохраняйте стиль** - бот дружелюбный и неформальный
2. **Адаптируйте, а не просто переводите** - учитывайте культурные особенности
3. **Тестируйте на носителях языка** - пусть проверят естественность текста
4. **Длина текста** - в некоторых языках тексты длиннее/короче
5. **Форматирование** - сохраняйте markdown разметку (`**жирный**`, `*курсив*`)

## Помощь сообщества

Если вы создали локализацию на новый язык:
1. Протестируйте её
2. Поделитесь с сообществом
3. Создайте pull request (если проект на GitHub)

---

Удачи с локализацией! 🌍
