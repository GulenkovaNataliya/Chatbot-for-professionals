# Как запустить бота "Вайб-Компас Освобождения"

## 🚀 Быстрый старт (Windows)

### Вариант 1: Двойной клик (рекомендуется)

1. **Найдите файл `run.bat`** в папке `vibe-compass-bot`
2. **Дважды кликните** по нему
3. Бот запустится автоматически!

Скрипт `run.bat` автоматически:
- Проверит наличие Python
- Проверит файл `.env` с токеном
- Установит зависимости (если нужно)
- Запустит бота

### Вариант 2: Через командную строку

1. Откройте **Командную строку (CMD)** или **PowerShell**
2. Перейдите в папку сботом:
   ```cmd
   cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
   ```
3. Запустите:
   ```cmd
   run.bat
   ```

## ❌ Проблема: Чат не работает

### Возможные причины и решения:

#### 1. **Бот не запущен**
**Решение:** Запустите бота через `run.bat` (см. выше)

#### 2. **Python не установлен или не в PATH**
**Симптом:** При запуске `run.bat` появляется ошибка "Python is not installed"

**Решение:**
- Скачайте Python с https://www.python.org/downloads/
- При установке обязательно поставьте галочку "Add Python to PATH"
- Перезапустите командную строку

#### 3. **Не установлены зависимости**
**Симптом:** Ошибка "ModuleNotFoundError: No module named 'telegram'"

**Решение:** Запустите `install.bat` или вручную:
```cmd
cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
python -m pip install -r requirements.txt
```

#### 4. **Неправильный токен бота**
**Симптом:** Ошибка при запуске "Unauthorized" или "Invalid token"

**Решение:**
1. Откройте файл `.env` в блокноте
2. Проверьте, что токен указан правильно:
   ```
   TELEGRAM_BOT_TOKEN=8395070199:AAFkcaKOrCuQCr07_lKV2SpcE-4EoxUI9xQ
   ```
3. Убедитесь, что нет лишних пробелов

#### 5. **База данных не инициализирована**
**Решение:**
```cmd
cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
python database.py
```

#### 6. **Бот работает, но не отвечает в Telegram**
**Проверьте:**
- Интернет-соединение
- Бот запущен и в консоли написано "✅ Бот запущен и готов к работе!"
- Вы пишете правильному боту в Telegram
- Попробуйте команду `/start`

## 🔧 Диагностика проблем

### Проверка Python:
```cmd
python --version
```
Должно показать: `Python 3.8` или выше

### Проверка зависимостей:
```cmd
python -m pip list
```
Должны быть установлены:
- python-telegram-bot
- python-dotenv
- sqlalchemy
- requests

### Проверка токена:
```cmd
cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
python -c "from config import TELEGRAM_BOT_TOKEN; print('Токен установлен:', 'Да' if TELEGRAM_BOT_TOKEN and TELEGRAM_BOT_TOKEN != 'your_bot_token_here' else 'Нет')"
```

### Полная переустановка:
Если ничего не помогает, выполните:

```cmd
cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"

REM Удаляем старую базу данных
del vibe_compass.db

REM Переустанавливаем зависимости
python -m pip install --upgrade pip
python -m pip install -r requirements.txt --force-reinstall

REM Инициализируем базу данных
python database.py

REM Запускаем бота
python bot.py
```

## 📝 Проверка логов

Когда бот запущен, в консоли будут видны все события:
- Подключения пользователей
- Ответы на вопросы
- Ошибки (если есть)

Если видите ошибки - сфотографируйте и покажите разработчику.

## 🆘 Всё ещё не работает?

1. Сделайте скриншот ошибки из консоли
2. Проверьте файл `.env` (удалите токен перед отправкой!)
3. Опишите, что именно не работает:
   - Бот не запускается?
   - Бот запускается, но не отвечает?
   - Бот отвечает, но с ошибками?

## ✅ Признаки правильной работы

Если всё работает, вы увидите в консоли:
```
Инициализация базы данных...
Создание приложения бота...
✅ Бот запущен и готов к работе!
Нажмите Ctrl+C для остановки
```

И в Telegram бот будет отвечать на команду `/start`.

---

**Важно:** Бот должен быть запущен постоянно, чтобы отвечать пользователям. Для остановки нажмите `Ctrl+C` в консоли.
