# Инструкция по развертыванию бота на PythonAnywhere

## Что такое PythonAnywhere?
PythonAnywhere - это облачный хостинг специально для Python приложений. У них есть **бесплатный план**, который отлично подходит для Telegram-ботов.

---

## Шаг 1: Регистрация на PythonAnywhere

1. Перейдите на сайт: **https://www.pythonanywhere.com/**
2. Нажмите **"Pricing & signup"** → **"Create a Beginner account"**
3. Заполните форму регистрации:
   - Username (будет частью URL)
   - Email
   - Password
4. Подтвердите email
5. Войдите в аккаунт

---

## Шаг 2: Загрузка проекта на PythonAnywhere

### Вариант A: Через Git (рекомендуется)

1. Создайте репозиторий на GitHub:
   - Перейдите на **https://github.com/new**
   - Назовите репозиторий: `vibe-compass-bot`
   - Сделайте его **приватным** (чтобы скрыть токен)
   - Нажмите "Create repository"

2. На вашем компьютере выполните команды:
   ```cmd
   cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
   git remote add origin https://github.com/ваш_username/vibe-compass-bot.git
   git branch -M main
   git push -u origin main
   ```

3. На PythonAnywhere откройте **Bash console** (на Dashboard):
   ```bash
   git clone https://github.com/ваш_username/vibe-compass-bot.git
   cd vibe-compass-bot
   ```

### Вариант B: Через загрузку ZIP-файла

1. На вашем компьютере:
   - Откройте папку проекта
   - Выделите все файлы **кроме** `.env`, `__pycache__`, `vibe_compass.db`
   - Нажмите правой кнопкой → "Отправить" → "Сжатая ZIP-папка"
   - Назовите `vibe-compass-bot.zip`

2. На PythonAnywhere:
   - Перейдите на вкладку **"Files"**
   - Нажмите **"Upload a file"**
   - Загрузите `vibe-compass-bot.zip`
   - В консоли Bash выполните:
     ```bash
     unzip vibe-compass-bot.zip -d vibe-compass-bot
     cd vibe-compass-bot
     ```

---

## Шаг 3: Настройка виртуального окружения

В Bash консоли PythonAnywhere выполните:

```bash
# Создание виртуального окружения
python3.11 -m venv venv

# Активация
source venv/bin/activate

# Обновление pip
pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt
```

---

## Шаг 4: Создание файла .env с токеном

**ВАЖНО:** Не загружайте файл `.env` на GitHub! Создайте его вручную на сервере.

В Bash консоли:

```bash
# Создание файла .env
nano .env
```

В открывшемся редакторе вставьте:
```
TELEGRAM_BOT_TOKEN=8395070199:AAFkcaKOrCuQCr07_lKV2SpcE-4EoxUI9xQ
```

Сохраните файл:
- Нажмите `Ctrl+O` (буква O, не ноль)
- Нажмите `Enter`
- Нажмите `Ctrl+X`

---

## Шаг 5: Инициализация базы данных

```bash
python database.py
```

Должно появиться: `✅ База данных инициализирована`

---

## Шаг 6: Тестовый запуск

Проверим, что бот работает:

```bash
python bot.py
```

Если всё работает, вы увидите: `✅ Бот запущен и готов к работе!`

Протестируйте бота в Telegram, отправив `/start`

Остановите бота: нажмите `Ctrl+C`

---

## Шаг 7: Настройка автоматического запуска (Always-On Task)

⚠️ **ВАЖНО:** На бесплатном плане PythonAnywhere Always-On задачи **НЕ доступны**.

### Решения для бесплатного плана:

#### Вариант 1: Scheduled Tasks (запуск по расписанию)

1. На PythonAnywhere перейдите на вкладку **"Tasks"**
2. Создайте новую задачу:
   - **Time:** Выберите время (например, каждый час)
   - **Command:**
     ```bash
     cd ~/vibe-compass-bot && source venv/bin/activate && python bot.py
     ```
3. Нажмите "Create"

⚠️ **Проблема:** Бот будет перезапускаться каждый час, что не идеально.

#### Вариант 2: Upgrade to paid plan ($5/месяц)

Платный план "Hacker" дает:
- Always-On задачи (бот работает 24/7 без остановок)
- Больше CPU времени
- Больше места на диске

Для перехода:
1. На Dashboard нажмите **"Account"**
2. Выберите **"Upgrade to Hacker"**
3. Оплатите $5/месяц

После оплаты:
1. Перейдите на вкладку **"Tasks"**
2. В разделе **"Always-on tasks"** нажмите **"Create a new always-on task"**
3. Введите команду:
   ```bash
   cd ~/vibe-compass-bot && bash start_bot.sh
   ```
4. Нажмите "Create"

#### Вариант 3: Использовать другой хостинг

Бесплатные альтернативы с Always-On:
- **Railway.app** - бесплатные $5 кредитов в месяц
- **Render.com** - бесплатный план для ботов
- **Fly.io** - бесплатные ресурсы

---

## Шаг 8: Мониторинг бота

### Просмотр логов:

```bash
cd ~/vibe-compass-bot
tail -f nohup.out  # Просмотр логов в реальном времени
```

### Проверка работы бота:

```bash
ps aux | grep bot.py  # Проверить, запущен ли процесс
```

### Перезапуск бота вручную:

```bash
cd ~/vibe-compass-bot
source venv/bin/activate
python bot.py
```

---

## Альтернативные хостинги (рекомендации)

### 1. Railway.app (рекомендуется для начала)

**Преимущества:**
- ✅ Бесплатные $5 кредитов в месяц
- ✅ Автоматическое развертывание из GitHub
- ✅ Бот работает 24/7
- ✅ Простая настройка

**Инструкция:**
1. Регистрация: https://railway.app/
2. "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Добавьте переменную окружения: `TELEGRAM_BOT_TOKEN`
5. Railway автоматически развернет бота

### 2. Render.com

**Преимущества:**
- ✅ Бесплатный план
- ✅ Автоматическое развертывание
- ✅ SSL сертификаты

**Минусы:**
- ⚠️ Бот "засыпает" после 15 минут неактивности

### 3. Fly.io

**Преимущества:**
- ✅ Бесплатные ресурсы
- ✅ Работает 24/7
- ✅ Хорошая производительность

**Минусы:**
- ⚠️ Требуется банковская карта для регистрации (не списывают деньги)

---

## Часто задаваемые вопросы

### Q: Бот перестал отвечать
**A:** Проверьте:
1. Запущен ли процесс: `ps aux | grep bot.py`
2. Логи на ошибки: `tail -f nohup.out`
3. Правильный ли токен в `.env`

### Q: База данных не сохраняется
**A:** Убедитесь, что файл `vibe_compass.db` создан и есть права на запись

### Q: Бот работает локально, но не на сервере
**A:** Проверьте:
1. Установлены ли все зависимости: `pip list`
2. Правильная ли версия Python: `python --version`
3. Есть ли файл `.env` с токеном

### Q: Как обновить бота после изменений?
**A:**
```bash
cd ~/vibe-compass-bot
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
# Перезапустите бота
```

---

## Рекомендации

**Для тестирования:** Используйте бесплатный PythonAnywhere со Scheduled Tasks

**Для продакшена:** Используйте Railway.app или платный план PythonAnywhere ($5/месяц)

---

## Контакты и поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Посмотрите документацию PythonAnywhere: https://help.pythonanywhere.com/
3. Проверьте статус Telegram API: https://status.telegram.org/

---

**Создано с помощью Claude Code** 🤖
