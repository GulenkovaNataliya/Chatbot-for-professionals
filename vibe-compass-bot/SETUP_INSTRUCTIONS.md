# Инструкция по запуску бота на Windows

## ✅ Что уже сделано:
- ✅ Файл `.env` создан с вашим токеном
- ✅ Созданы скрипты `install.bat` и `run.bat`

## 📋 Шаги для запуска:

### Вариант 1: Использование batch-файлов (рекомендуется)

1. **Откройте проводник Windows** и перейдите в папку:
   ```
   C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot
   ```

2. **Двойным кликом запустите файл** `install.bat`
   - Этот скрипт установит все зависимости и инициализирует базу данных
   - Дождитесь сообщения "Installation completed successfully!"

3. **Двойным кликом запустите файл** `run.bat`
   - Этот скрипт запустит бота
   - Вы должны увидеть: "✅ Бот запущен и готов к работе!"

4. **Протестируйте бота** в Telegram:
   - Найдите вашего бота по имени
   - Отправьте команду `/start`

---

### Вариант 2: Ручная установка через Command Prompt

1. **Откройте Command Prompt** (cmd):
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter

2. **Перейдите в папку проекта**:
   ```cmd
   cd /d "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
   ```

3. **Проверьте версию Python**:
   ```cmd
   python --version
   ```
   Должно показать версию Python 3.8 или выше

4. **Обновите pip**:
   ```cmd
   python -m pip install --upgrade pip
   ```

5. **Установите зависимости**:
   ```cmd
   python -m pip install -r requirements.txt
   ```
   Подождите, пока установятся все пакеты (может занять 1-2 минуты)

6. **Инициализируйте базу данных**:
   ```cmd
   python database.py
   ```
   Должно появиться сообщение: "✅ База данных инициализирована"

7. **Запустите бота**:
   ```cmd
   python bot.py
   ```
   Должно появиться: "✅ Бот запущен и готов к работе!"

8. **Протестируйте бота** в Telegram

---

### Вариант 3: Использование PowerShell

1. **Откройте PowerShell**:
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell"

2. **Перейдите в папку проекта**:
   ```powershell
   cd "C:\Users\Natalia\Documents\vibe-code\Chatbot for professionals\vibe-compass-bot"
   ```

3. **Выполните установку**:
   ```powershell
   python -m pip install --upgrade pip
   python -m pip install -r requirements.txt
   python database.py
   python bot.py
   ```

---

## 🔍 Проверка статуса установки

После выполнения установки, в папке проекта должны появиться:
- ✅ Файл `.env` (уже создан)
- ✅ Файл `vibe_compass.db` (база данных)
- ✅ Папка `__pycache__` (кэш Python)

---

## ❓ Возможные проблемы и решения

### Проблема: "python is not recognized"
**Решение**: Python не установлен или не добавлен в PATH. Установите Python с python.org

### Проблема: "No module named 'telegram'"
**Решение**: Зависимости не установлены. Выполните: `python -m pip install -r requirements.txt`

### Проблема: "Токен бота не установлен"
**Решение**: Проверьте файл `.env` - токен уже должен быть там

### Проблема: Бот не отвечает в Telegram
**Решение**:
- Убедитесь, что `python bot.py` запущен и работает
- Проверьте, что токен правильный
- Проверьте интернет-соединение

---

## 📱 Тестирование бота

После запуска `python bot.py`:

1. Откройте Telegram
2. Найдите вашего бота (по username, который вы указали в @BotFather)
3. Отправьте `/start`
4. Следуйте инструкциям бота

### Команды бота:
- `/start` - Начать диагностику
- `/help` - Справка
- `/cancel` - Отменить текущий опрос
- `/stats` - Статистика (для админов)

---

## 🛑 Остановка бота

Чтобы остановить бота:
- В окне где запущен `python bot.py`, нажмите `Ctrl + C`

---

## 🚀 Следующие шаги

После успешного запуска вы можете:
- Изменить тексты в `messages.py` под свои нужды
- Настроить интеграцию с CRM в `crm_integration.py`
- Перевести бота на другой язык (см. `LOCALIZATION.md`)
- Настроить автозапуск бота при старте системы

---

**Создано с помощью Claude Code**
