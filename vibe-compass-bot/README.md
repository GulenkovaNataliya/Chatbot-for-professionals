# Вайб-Компас Освобождения

Telegram-бот для диагностики рутинных проблем в бизнесе и генерации персонализированных рекомендаций по автоматизации.

## Описание проекта

Это не просто чат-бот - это цифровой проводник, который через серию вопросов помогает предпринимателям и владельцам бизнеса:
- Выявить самые болезненные рутинные процессы
- Получить конкретный план автоматизации
- Оценить потенциальную экономию времени
- Записаться на консультацию с экспертом

## Структура проекта

```
vibe-compass-bot/
├── bot.py                 # Главный файл запуска бота
├── config.py              # Конфигурация и константы
├── database.py            # Работа с базой данных
├── handlers.py            # Обработчики команд и кнопок
├── keyboards.py           # Клавиатуры с кнопками
├── messages.py            # Тексты сообщений (для локализации)
├── crm_integration.py     # Интеграция с CRM системами
├── requirements.txt       # Зависимости Python
├── .env.example           # Пример файла с переменными окружения
└── README.md              # Документация
```

## Установка и запуск

### Шаг 1: Клонирование проекта

```bash
cd vibe-compass-bot
```

### Шаг 2: Создание виртуального окружения (рекомендуется)

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Создание бота в Telegram

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите **токен бота**
4. Сохраните этот токен - он понадобится в следующем шаге

### Шаг 5: Настройка конфигурации

1. Скопируйте файл `.env.example` в `.env`:
```bash
copy .env.example .env    # Windows
cp .env.example .env      # Linux/Mac
```

2. Откройте файл `.env` и вставьте свой токен бота:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### Шаг 6: Инициализация базы данных

```bash
python database.py
```

Вы должны увидеть сообщение: `✅ База данных инициализирована`

### Шаг 7: Запуск бота

```bash
python bot.py
```

Если все настроено правильно, вы увидите:
```
✅ Бот запущен и готов к работе!
Нажмите Ctrl+C для остановки
```

## Использование бота

### Команды пользователя

- `/start` - Начать диагностику
- `/help` - Справка по использованию
- `/cancel` - Отменить текущий опрос

### Команды администратора

- `/stats` - Показать статистику использования бота

### Сценарий работы

1. **Приветствие** - Бот предлагает пройти 2-минутный тест
2. **Вопрос об эмоциях** - Определяет эмоциональное состояние
3. **Выявление проблемы** - Находит главный источник рутины
4. **Оценка времени** - Считает потери времени в часах
5. **Персонализированный инсайт** - Показывает конкретные решения
6. **Предложение** - Консультация / PDF-план / Отложить

## Интеграция с CRM

Бот поддерживает интеграцию с различными CRM системами для автоматической отправки лидов.

### AmoCRM

1. Получите токен доступа в AmoCRM
2. Добавьте в `.env`:
```
AMOCRM_TOKEN=ваш_токен
AMOCRM_DOMAIN=yourdomain.amocrm.ru
```

### Bitrix24

Функционал готовится к реализации. См. `crm_integration.py`

### Google Sheets

Простое решение для старта без настройки CRM. См. `crm_integration.py`

## Локализация (перевод на другие языки)

Все тексты бота находятся в файле `messages.py`. Чтобы создать версию на другом языке:

### Вариант 1: Создание отдельного файла

1. Создайте файл `messages_el.py` (для греческого) или `messages_en.py` (для английского)
2. Скопируйте содержимое `messages.py`
3. Переведите все тексты на нужный язык
4. В `handlers.py` измените импорт:
```python
# Было
from messages import *

# Стало
from messages_el import *  # для греческого
```

### Вариант 2: Система выбора языка

```python
# config.py
LANGUAGE = 'ru'  # 'ru', 'en', 'el'

# handlers.py
if LANGUAGE == 'ru':
    from messages import *
elif LANGUAGE == 'el':
    from messages_el import *
elif LANGUAGE == 'en':
    from messages_en import *
```

### Пример перевода на греческий

Файл `messages_el.py`:
```python
START_MESSAGE = """Γεια σου, ελευθερωτή! 👋

Αισθάνομαι ότι έχεις κολλήσει στη ρουτίνα...
"""
```

## Расширенные возможности

### Добавление напоминаний

Бот поддерживает отложенные напоминания через `JobQueue`. Раскомментируйте код в `handlers.py`:

```python
# Установка напоминания через 24 часа
context.job_queue.run_once(send_reminder, 86400, data=user_id)
```

### Отправка PDF-файлов

Создайте PDF-план и добавьте в код:

```python
async def pdf_callback(update, context):
    # ...
    await query.message.reply_document(
        document=open('plan.pdf', 'rb'),
        filename='5_idey_dlya_vaibkodinga.pdf'
    )
```

### Аналитика и A/B тесты

База данных уже сохраняет все ответы пользователей. Используйте `database.py` для анализа:

```python
from database import get_statistics

stats = get_statistics()
print(f"Конверсия: {stats['completion_rate']}%")
print(f"Популярные проблемы: {stats['pain_points']}")
```

## Деплой на сервер

### Вариант 1: Heroku

1. Установите Heroku CLI
2. Создайте `Procfile`:
```
worker: python bot.py
```
3. Деплой:
```bash
heroku create your-bot-name
git push heroku main
heroku ps:scale worker=1
```

### Вариант 2: VPS (DigitalOcean, Timeweb и т.д.)

1. Подключитесь к серверу
2. Установите Python и зависимости
3. Запустите бота через `screen` или `tmux`:
```bash
screen -S bot
python bot.py
# Нажмите Ctrl+A, затем D для выхода
```

### Вариант 3: Systemd (Linux)

Создайте сервис `/etc/systemd/system/vibe-bot.service`:
```ini
[Unit]
Description=Vibe Compass Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/vibe-compass-bot
ExecStart=/path/to/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl enable vibe-bot
sudo systemctl start vibe-bot
sudo systemctl status vibe-bot
```

## Мониторинг и логи

Бот логирует все события. Для просмотра логов:

```python
# В bot.py уже настроен logging
# Логи выводятся в консоль

# Для сохранения в файл добавьте:
logging.basicConfig(
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler()
    ],
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
```

## Частые проблемы и решения

### Ошибка: "Токен бота не установлен"
**Решение:** Проверьте файл `.env` и убедитесь, что `TELEGRAM_BOT_TOKEN` заполнен

### Ошибка: "ModuleNotFoundError"
**Решение:** Установите зависимости: `pip install -r requirements.txt`

### Бот не отвечает
**Решение:**
- Проверьте, что бот запущен (`python bot.py`)
- Убедитесь, что токен правильный
- Проверьте интернет-соединение

### Ошибка базы данных
**Решение:** Удалите файл `vibe_compass.db` и запустите `python database.py` снова

## Лицензия

Этот проект создан в образовательных целях.

## Контакты и поддержка

Если у вас возникли вопросы или предложения:
- Создайте Issue в репозитории
- Напишите разработчику

---

Создано с помощью Claude Code 🤖
